'use client';
import React from 'react';

const Links = [
  {
    name: 'Home',
    href: '/',
  },
];

const NavLink = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  <>{children}</>;
};

export default function Navbar() {
  return <></>;
}
